{
  "version": 2,
  "installCommand": "npm install && npm install --prefix frontend",
  "buildCommand": "npm install --prefix frontend && GENERATE_SOURCEMAP=false npm run build --prefix frontend",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
